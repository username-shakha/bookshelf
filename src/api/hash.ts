// // Импортируем CryptoJS
// import CryptoJS from "crypto-js";

// const hashed = (stringToHash: string) => {
//   //   const method = "GET";
//   //   const url = "https://example.com/api";
//   //   const body = '{"key": "value"}';
//   //   const userSecret = "MySecret";

//   //   const stringToHash = `${method}+${url}+${body}+${userSecret}`;

//   const hash = CryptoJS.MD5(stringToHash).toString();

//   return hash;
// };

// export default hashed;
